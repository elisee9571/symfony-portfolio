{% set route_name = app.request.attributes.get('_route') %}
<div class="container-fluid">
	<header class="d-flex justify-content-center py-3">
		<ul class="nav nav-pills">
			<li class="nav-item">
				<a href="{{ path('home_index') }}" {% if route_name == 'home_index' %} class="nav-link active" {% else %} class="nav-link" {% endif %}>Accueil</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('project_index') }}" {% if route_name == 'project_index' %} class="nav-link active" {% else %} class="nav-link" {% endif %}>Réalisations</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('about_index') }}" {% if route_name == 'about_index' %} class="nav-link active" {% else %} class="nav-link" {% endif %}>À Propos</a>
			</li>
		</ul>
	</header>
</div>
